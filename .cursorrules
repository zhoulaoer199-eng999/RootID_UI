# RootID_UI_v2 - Cursor 开发规范

## 核心原则
- 拒绝过度设计：优先 StatelessWidget，严禁无意义封装
- 极简依赖：禁止添加新包，优先 Flutter 原生组件
- 扁平化：Widget 树尽量扁平，避免深层嵌套

## 编码规范
- 命名：类用 PascalCase，变量/函数用 camelCase
- 样式：禁止硬编码颜色，必须使用 Theme.of(context)
- 空安全：严禁随意使用 ! 强制断言
- 布局：优先 Column/Row/Stack + Expanded/Flexible

## Cursor 使用规范
- 修改文件时：必须输出完整代码，禁止省略
- 沟通语言：注释和解释使用简体中文
- 零废话：直接给出代码实现，不要解释过程

## 项目结构
- 页面：lib/pages/*.dart
- 组件：lib/widgets/*.dart
- 服务：lib/services/*.dart (mock 模式)
- 常量：lib/utils/constants.dart

## 禁止操作
- ❌ 修改 pubspec.yaml（除非明确批准）
- ❌ 删除已有的 const 关键字
- ❌ 硬编码颜色/字号（必须用 Theme）
- ❌ 输出省略号或 // ... rest of code
